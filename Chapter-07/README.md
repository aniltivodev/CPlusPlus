On 25-Jan-2026:
We learned:
  1. Compound Statements (Blocks)
    a. Using blocks to execute multiple statements conditionally
    b. Block nesting levels
  2. User defined namespaces and the scope resolution operator
    a. Accessing a namespace with a scope resolution operator
    b. Using the scope resolution from within a namespace
    c. Forward declaration of content in namespace
    d. Multiple namespaces blocks are allowed
    e. Nested namespaces
    f. Namespace aliases
    g. How to use namespaces
  3. Local variables
    a. Local variables have block scope
    b. All variables with in a block must be unique
    c. Local variables have automatic storage duration
    d. Local variable in a nested block
    e. Local variable have no linkage
    f. Variables should be defined in the most limited scope
  4. Introduction to global scope
    a. Declaring global variables
    b. The scope of global variables
    c. Global variables have static scope
    d. Naming global variables
    e. Global variable initialization
    f. Constant global variables
    g. A word of causion about (non-constant) Global variables
  5. Variable shadowing (Name hidding)
    a. Shadowing of global variables
    b. Avoid variable shadowing
  6. Internal Linkage
    a. Global variables with internal linkage
    b. Functions with internal linkage
    c. The one definition rule and internal linkage
    d. Static vs unnamed namespaces
    e. Why bother giving identifiers internal linkage?
