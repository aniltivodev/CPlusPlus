On 08-Jan-2026:
We learned:
  1. Bit flags and bit manipulation via std::bitset
    a. Bit numbering and bit positions.
    b. Manipulating bits via std::bitset
    c. What if we want to get or set multiple bits at once
    d. The size of std::bitset
    e. Querying std::bitset
  2. Bitwise Operator
    a. Bitwise left(<<) or Bitwise right operators
    b. Bitwise NOT
    c. Bitwise OR
    d. Bitwise AND
    e. Bitwise XOR
    f. Bitwise assignment operator
    g. Bitwise operators perform integral promotion on Smaller integrall types
  3. Bit manipulation with bitwise operators and bit masks
    a. Bit masks
    b. Defining bit masks in C++14
    c. Defining bit masks in C++11 or earlier
    d. Testing a bit
    e. Setting a bit
    f. Resetting a bit
    g. Flipping a bit
    h. Bit masks and std::bitset
    i. Marking bit masks meaningful
    j. When are bit flags useful
    k. bit masks involving multiple bits
  4. Converting integral between binary and decimal representations
