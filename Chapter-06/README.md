On 04-Jan-2026:
We learned:
  1. Operator Precedece and Associativity
    a. Evaluation of compound expression
    b. Operator Precedence
    c. Operator Associativity
    d. Parenthesization
    e. Value computation of operations
    f. Evaluation of operands
    g. The order of operands(including function arguments) is modtly unspecified
  2. Arithmetic operators
    a. Binary Arithmetic operators
    b. Unary Arithmetic operators
    c. Integer and floating point division
    d. Using static_cast<> to do floating point division with integer
    e. Dividing by 0 or 0.0
    f. Arithmetic assignment operators
    g. Modifying and non modifying operators
  3. Remainer and Exponentiation
    a. Remainer with negative numbers
    b. Where's the exponent operator?
  4. Increment and Decrement operators and side effects
    a. Incrementing and Decrementing variables
    b. Prefix increment and decrement
    c. Postfix increment and decrement
    d. When to use prefix and postfix
    e. The sequencing of side effects
  5. The comma operator
    a. Comma as a separator
  6. The Conditional Operator
    a. The conditional operator eveluates as part of an expression
    b. Parenthesizing the conditional operator
    c. The type of the expression must match or be convertible
  7. Relational operators and floating point comparisons
    a. Boolean conditional values
    b. Comparison of calculated floating point values can be problematic
    c. Floating point less than and greater than
    d. Floating point equality and inequality
    e. Comparing floating point numbers
  8. Logical Operators
    a. Logical NOT
    b. Logical OR
    c. Logical AND
    d. Short circuit evaluation
    e. Micing ANDs and ORs
    f. De Morgan's Laws
    g. Where's the logical exclusive or (XOR) operator?
    h. Alternative operator representations.
