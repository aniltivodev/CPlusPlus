On 15-Dec-2025:
We learned:
  1. Introduction to fundamental data types
    a. Bits, bytes and memory addressing
    b. data types
    c. fundamental data types
    d. Integer and integral types
    e. Other sets of types
    f. The "_t" suffix
  2. Void
    a. Functions that do not return a value
    b. Deprecated: Functions that do not take parameters
    c. Other uses of Void
  3. Objects Sizes and the sizeof operator
    a. Obejct Sizes
    b. Fundamental data type sizes
    c. The sizeof operator
    d. Fundamental data type performance
  4. Signed Data Types
    a. Defining signed integers
    b. Signed integer ranges
    c. overflow
    d. Integer division
  5. Unsigned Integers and why to avoid them
    a. Defining unsigned integer
    b. Remembering the terms signed and unsigned
    c. unsigned integer overflow
    d. The controversey over signed numbers
    e. So, when should you use unsigned number?
  6. Fixed width integers and size_t
    a. Why isn't the size of the integer type fixed?
    b. Fixed width integer
    c. Other fixed width downsides
    d. Best practices for integeral types
    e. What is std:size_t
    f. The size_of operator returns a value of type std:size_t
    g. std:size_t imposes an upper limit on the size of an object
  7. Introduction to scientific notation
    a. Significant Digit
    b. How to convert decimal number to scientific notation.
  8. Floating point numbers
    a. C++ floating point types
    b. Floating point variables and literals
    c. Floating point range
    d. Floating point precision
    e. Outputing floating point values
    f. Rouding errors make floating point comparison tricky
    g. NaN and Inf
  9. Boolean values
    a. Boolean variable
    b. Printing boolean values
    c. Use std::boolalpha to print true or false
    d. Inputting Boolean values
    e. Boolean return values
  10. Introduction to if statements
    a. if_else
    b. Chaining if statements
    c. Boolean return values and if statements
    d. Non boolean conditionals
  11. Chars
    a. init chars
    b. Printing chars
    c. Extracting white spaces characters
    d. char size, range and default sign
    e. Excape sequence
    f. what are chat_t, char8_t, char16_t and char32_t?
  12. Introduction to type conversion and static_cast
    a. Implicit type conversion
    b. Type conversion of a value produces a new value
    c. Implicit type conversion warning
    d. An introduction to explicit type conversion via the static_cast operator
    e. Sign conversion using static_cast
  13. Chapter 4 Summary and Quizes
