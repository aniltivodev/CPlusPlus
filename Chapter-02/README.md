On 14-Dec-2025:
We learned:
  1. Intriduction to functions
    a. Nested functions are not allowed
  2. Function return values
    a. Return Values
    b. Revisiting main()
    c. Status Code
    d. Functions can Return only a single value
    e. The function author can decide what the return value means
    f. Reusing functions
  3. Void function (non-value returning functions)
    a. Void return value
    b. Void function does not need a return statement
    c. Void can not be used in an axpression that require a value
  4. introduction to function parameters and arguments
    a. How parameters and arguments work
    b. Using return values as arguments
    c. How parameters and returns work together
    d. Unrefferenced parameters and unnamed parameters
  5. Introduction to local scope:
    a. Local variables
    b. Local variable lifetime
    c. What happens when an object destroyed?
    d. Local scope(block scope)
    e. Out of scope v/s going out of scope
    f. functional separation
    g. where to define local variables
    h. introduction to temporary objects
  6. Why functions are usedful and how to use them effectively
    a. Why use functions?
    b. effectively using functions
  7. Forward declarations and definitions
    a. Why forward declaration?
    b. Other type of forward declaration
    c. Declarations v/s Definitions
    e. The one definition rule.
  8. Program with multiple code files
  9. Name Collisions and an introduction to namespaces
    a. Scope regions
    b. Name spaces
    c. The std namespaces
    d. Using namespace std (and why to avoid it?)
    e. curly braces and indented code
  10. Introduction to preprocessor
    a. Preprocessor directives
    b. #include
    c. macro defines
    d. Objects like macro and substitution text
    e. Objects like macros without substitution text
    f. Conditional Compilation
    g. #if 0
    h. Macro substitution within other preprocessor commands
    i. The scope of #defines
